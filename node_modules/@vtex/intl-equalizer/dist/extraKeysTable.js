'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createTable;

var _cliTable = require('cli-table3');

var _cliTable2 = _interopRequireDefault(_cliTable);

var _safe = require('colors/safe');

var _safe2 = _interopRequireDefault(_safe);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function createTable(result, referenceLocale) {
  const table = new _cliTable2.default({
    chars: {
      top: '═',
      'top-mid': '╤',
      'top-left': '╔',
      'top-right': '╗',
      bottom: '═',
      'bottom-mid': '╧',
      'bottom-left': '╚',
      'bottom-right': '╝',
      left: '║',
      'left-mid': '╟',
      mid: '─',
      'mid-mid': '┼',
      right: '║',
      'right-mid': '╢',
      middle: '│'
    },
    head: [{
      colSpan: 2,
      hAlign: 'center',
      content: _safe2.default.yellow(`EXTRA KEYS \n These keys are missing from the reference locale.`)
    }]
  });

  table.push([{
    colSpan: 2,
    hAlign: 'center',
    content: _safe2.default.yellow(`REFERENCE LOCALE: ${referenceLocale.toUpperCase()}`)
  }]);

  table.push([{ hAlign: 'center', content: _safe2.default.yellow('LOCALE') }, { hAlign: 'center', content: _safe2.default.yellow('EXTRA KEYS') }]);

  Object.keys(result).forEach(countryName => {
    if (countryName === referenceLocale) return;

    const country = countryName.toUpperCase();

    result[countryName].extraKeys.forEach(currentKey => {
      table.push([country, currentKey]);
    });
  });

  console.log(table.toString());
}