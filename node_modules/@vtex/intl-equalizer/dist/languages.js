'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getAvailableLanguages = getAvailableLanguages;

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _constants = require('./constants');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getAvailableLanguages({ directory, filesToIgnore = [] }) {
  let languages = [];

  try {
    languages = _fs2.default.readdirSync(directory).filter(fileName => (fileName.includes('.json') || fileName.includes('.js')) && !filesToIgnore.includes(fileName)).map(fileName => {
      if (fileName.includes('.json')) return fileName.replace('.json', '');
      if (fileName.includes('.js')) return fileName.replace('.js', '');
    });

    if (languages.length === 0) {
      return { error: _constants.ERRORS.ERROR_NO_LOCALE_FILES };
    }
  } catch (e) {
    return { error: _constants.ERRORS.ERROR_NO_LOCALE_FOLDER };
  }

  return languages.reduce((splittedLocales, locale) => {
    if (locale.indexOf('-') > 0) {
      splittedLocales.regionLocales.push(locale);
    } else {
      splittedLocales.generalLocales.push(locale);
    }

    return splittedLocales;
  }, {
    generalLocales: [],
    regionLocales: []
  });
}