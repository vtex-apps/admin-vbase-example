'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = configure;

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function configure() {
  // eslint-disable-next-line @typescript-eslint/no-var-requires
  const packageJson = require(_path2.default.join(process.cwd(), 'package.json'));

  const referenceLocaleFromPackageJson = packageJson['intl-equalizer'] && packageJson['intl-equalizer'].referenceLocale;

  const localesDirectoryFromPackageJson = packageJson['intl-equalizer'] && packageJson['intl-equalizer'].localeDirectory;

  const filesToIgnoreFromPackageJson = packageJson['intl-equalizer'] && packageJson['intl-equalizer'].filesToIgnore;

  const referenceLocale = referenceLocaleFromPackageJson || 'en';

  const localesDirectory = localesDirectoryFromPackageJson && _path2.default.join(process.cwd(), localesDirectoryFromPackageJson) || _path2.default.join(process.cwd(), 'messages');

  const filesToIgnore = filesToIgnoreFromPackageJson || [];

  return {
    localesDirectory,
    referenceLocale,
    filesToIgnore
  };
}