'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = writeFile;

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _constants = require('./constants');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function writeFile({
  sortedLanguages,
  failedSortLanguages,
  locales,
  localesDirectory,
  referenceLocale
}) {
  locales.forEach(locale => {
    if (locale === referenceLocale || failedSortLanguages.find(failedLocale => failedLocale === locale)) {
      return;
    }

    const content = JSON.stringify(sortedLanguages[locale], null, 2);

    try {
      console.log(_constants.MESSAGES.WRITING_LOCALE(locale));
      _fs2.default.writeFileSync(`${localesDirectory}/${locale}.json`, content, 'utf8');
    } catch (err) {
      console.error(err);
      console.log(_constants.MESSAGES.COULD_NOT_WRITE_JSON(locale));
      process.exit(1);
    }
  });
}