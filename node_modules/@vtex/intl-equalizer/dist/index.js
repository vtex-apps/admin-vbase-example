#!/usr/bin/env node
'use strict';

var _commander = require('commander');

var _commander2 = _interopRequireDefault(_commander);

var _start = require('./start');

var _start2 = _interopRequireDefault(_start);

var _fix = require('./fix');

var _fix2 = _interopRequireDefault(_fix);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

_commander2.default.option('-f, --fix').option('-a, --all', 'Show all errors.').parse(process.argv);

if (_commander2.default.fix) {
  (0, _fix2.default)();
}

if (_commander2.default.all) {
  (0, _start2.default)({ all: true });
}

if (_commander2.default.args.length < 1) {
  (0, _start2.default)();
}