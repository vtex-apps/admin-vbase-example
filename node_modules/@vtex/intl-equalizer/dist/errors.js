'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = throwError;

var _constants = require('./constants');

function throwError(error, data) {
  switch (error) {
    case _constants.ERRORS.ERROR_NO_LOCALE_FILES:
      {
        console.error(_constants.MESSAGES.NO_LOCALE_FILES(data));
        process.exit(1);
        break;
      }

    case _constants.ERRORS.ERROR_NO_LOCALE_FOLDER:
      {
        console.error(_constants.MESSAGES.NO_LOCALE_FOLDER(process.cwd()));
        process.exit(1);
        break;
      }

    case _constants.ERRORS.ERROR_NO_KEYS_LOCALE:
      {
        console.error(_constants.MESSAGES.ERROR_NO_KEYS_LOCALE(data));
        process.exit(1);
        break;
      }
  }
}